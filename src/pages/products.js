import React from "react";
import { Heading, StyledPage, Title } from "../styles/styles";

function Products() {
    return (
        <StyledPage>
            <Heading>Products</Heading>
            <Title>Where you can find my current products.</Title>
        </StyledPage>
    );
}

export default Products;
